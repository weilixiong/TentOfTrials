# Independent log parser fixtures

Hand-written representative log lines for JSON, plain text, and nginx formats.

These samples were **not** generated by `LogParser` or any production code path.
They exist so parser tests cannot false-pass by round-tripping parser-produced data.

Also includes malformed lines that parsers must absorb without crashing.
